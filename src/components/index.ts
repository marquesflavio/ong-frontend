/**
 * É bom criar um index da pasta para organizar imports, e como pode ver no exemplo abaixo, isso ia resultar em um Botao/Botao, porque a pasta não tem index.
 * Sempre é bom ter index, se for só componente e estilos pode usar index.tsx e styles.css para cada pasta; S
 * Se houver mais coisas pode usar um index.ts, o nome do componente com tsx e o que tiver de extra junto. VOu deixar um exemplo chamado
 * ExampleComponent
 */

// export * from "./Botao/Botao";

/**
 * Um dica a mais, busque usar nomenclartura em inglês.
 */

